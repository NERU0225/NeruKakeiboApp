# 家計簿PWA 引き継ぎドキュメント

このファイルは、別のセッションで開発を続けるための引き継ぎ資料です。
**必ず `index.html` と `sw.js` の実物とセットで読んでください。**
実装の詳細はコードが正となります。このファイルには、コードを読んでも分からない
「なぜそうしたか」「何を守るべきか」を書いてあります。

現在のバージョン: **v2.1**（`index.html` の `APP_VERSION` と `sw.js` の `CACHE` が一致している必要あり）

---

## 1. これは何か

iPhoneで使う個人用の家計簿アプリ。単一HTMLファイルのPWA。
GitHub Pagesで公開し、Safariから「ホーム画面に追加」して使う。

- 公開URL: `https://neru0225.github.io/NeruKakeiboApp/`
- リポジトリ: `neru0225/NeruKakeiboApp`
- 利用者は開発者本人ひとり。マネーフォワード風のUI。

---

## 2. 絶対に壊してはいけない制約

### 2-1. 機内モード・弱電波でも即座に開くこと
利用者の最優先要求。**この方針は変更しない。**

- アプリ本体に通信コード（`fetch` / `XMLHttpRequest`）を**1つも入れない**。
  改修のたびに `grep -c "fetch(\|XMLHttpRequest" index.html` が 0 であることを確認する。
- Service Worker はキャッシュ優先（stale-while-revalidate）。
  キャッシュがあれば通信を待たず即返す。裏の更新取得は3秒でタイムアウト。
- 更新の反映は「次回起動時」。これは速度とのトレードオフとして合意済み。

### 2-2. データはすべて端末内（localStorage）
- キー: `kakeibo:meta`（設定）、`kakeibo:y:2026`（年別データ）
- サーバーもクラウド同期もない。バックアップはJSONの書き出し／読み込みのみ。

### 2-3. `prompt()` を使わない
Claudeアプリのプレビューなど、サンドボックス環境ではブロックされて無反応になる。
入力が必要なら画面内に入力欄を出す方式にする。
（`confirm()` は本番PWAでは動くので削除の確認などに残っているが、プレビューでは効かない）

---

## 3. バージョンとデプロイ手順

### 3-1. バージョンの付け方（重要）
**整数＝実際に導入した版、小数＝検証中の版。**

| 場面 | バージョン |
|---|---|
| 現在の導入済み | v1 |
| 修正を渡す | v1.1 |
| さらに修正（まだ導入していない） | v1.2 |
| 利用者が「導入するのでバージョンアップして」と言った | **v2** |

- 修正を渡すときは小数を上げる。整数を勝手に上げない。
- 改修ごとに必ず番号を上げる。**同じ番号で中身の違うものを渡さない**
  （利用者が判別できなくなる）。

#### 「導入するのでバージョンアップして」と言われたときの扱い
**動作に関わる変更は一切しない。バージョンに関する記述だけを更新する。**
利用者はすでに動作確認を終えているので、ここで挙動が変わると確認が無駄になる。

変更してよいのは、次の4点だけ。
1. `index.html` の `APP_VERSION` を新しい整数版にする
2. `sw.js` の `CACHE` を同じ番号にする
3. `CHANGELOG` のいちばん上の版の `released` に導入日を入れる
4. `CHANGELOG` の先頭に、次の検証用の枠 `{ver:'v3', released:'', items:[]}` を足す

**やってはいけないこと**：ロジックの修正、UIの調整、文言の変更、
「ついでに直しておく」たぐいの一切。勝手に手を入れない。

#### このときバグを見つけたら、バージョンアップを止める
整数版に上げる＝正式導入なので、**バグを抱えたまま上げてはいけない**。
黙って上げることも、黙って直すことも、どちらもしない。次の順で進める。

1. **待ってもらう。** 「バージョンアップの前に確認したいことがあります」と伝える
2. 見つけたバグの内容と、それが起きる条件を具体的に報告する
3. 修正案を示す（何をどう直すか、影響範囲はどこか）
4. **利用者の指示を待つ。** 勝手に修正しない
5. 「修正して」と言われたら、小数版として直して渡す（例：v1.6）
6. 動作確認後、改めて「バージョンアップして」と言われた時点で整数版に上げる

利用者が「そのまま上げていい」と判断した場合は、その指示に従って上げる。
判断するのは利用者であって、こちらではない。

### 3-2. 手順
1. `index.html` と `sw.js` を差し替える（この2つ以外はほぼ変更しない）
2. **必ずバージョンを上げる**
   - `index.html` の `const APP_VERSION='vN'`
   - `sw.js` の `const CACHE = 'kakeibo-vN'`
   - 両者は必ず同じ番号にする。上げ忘れると新版が反映されない。
3. GitHubで同名アップロードして上書き → Commit
4. アプリを終了 → 通信ありで開き直す（SWの仕組みを変えた回は2回必要なことがある）
5. ホーム画面上部の左に出る `vN` で反映を確認する

バージョン表示は「アプリが最新かどうか」を利用者が判別するために付けた。

### 3-3. バージョン履歴のデータ構造
`index.html` の `CHANGELOG` に持ち、ヘッダー左のバージョン表示をタップすると開く。
整数版ごとにまとめ、その版に入った小数版を `items` に並べる。

```
{ver:'v2', released:'',           items:[['v1.5','…'],['v1.4','…']]}  ← 検証中
{ver:'v1', released:'2026-08-02', items:[['','…']]}                    ← 導入済み
```

- `released` が空 → 画面に「検証中」と出る。日付が入っていれば「8/2 導入」と出る。
- **`items` が空の版は画面に出ない。** 導入直後に作る次の検証枠は、
  最初の改修が入るまで表示されない（検証しない期間があるため）。
- `items` の1つ目はバージョン番号、2つ目は説明。番号を空にすると番号なしの行になる
  （v1 以前の経緯をまとめた部分で使っている）。
- **改修したら**、いちばん上（検証中）の版の `items` の先頭に1行足す。
- 導入時の扱いは 3-1 を参照（ここには書かない。二重に書くと食い違うため）。

## 4. ファイル構成

| ファイル | 役割 | 変更頻度 |
|---|---|---|
| `index.html` | アプリ本体（HTML/CSS/JS 全部入り、約2900行） | ほぼ毎回 |
| `sw.js` | Service Worker（キャッシュ制御） | バージョン番号のみ |
| `manifest.json` | PWA設定 | ほぼ変更なし |
| `icon-180.png` / `icon-192.png` / `icon-512.png` | アイコン | 変更なし |
| `README.md` | 公開手順のメモ | 変更なし |

---

## 5. データ構造

### meta（`kakeibo:meta`）
```
{
  cats:    { expense:[{name,icon,color,subs[]}], income:[...] },
  methods: { expense:[{name,kind,closingDay,payDay,adjust}], income:[...] },
           // kind: cash / bank / card / emoney
           // closingDay,payDay は kind==='card' のみ
           // adjust は kind==='emoney' の残高調整値（累積）
  rates:   [8,10],          // 消費税率
  defaultRate: 10,
  fixed:   [ 固定費 ],       // 下記参照
  years:   [2026, ...],
  lastBackupAt, lastBackupCount, lastBackupCounts
}
```

### 年別データ（`kakeibo:y:2026`）
```
{ entries:[ 記録 ], budgets:{'2026-08':200000}, catBudgets:{'2026-08':{食費:20000}} }
```

### 記録（entry）
```
{
  id, date:'2026-08-01', type:'expense'|'income',
  store, memo, items:[ 明細 ],
  method:'現金',            // 支払い方法／収入経路（必須）
  chargeTo:'PayPay',        // チャージ先（電子マネーへのチャージのとき）
  expense:true,             // 経費（立替）フラグ
  status:'unclaimed'|'claimed'|'settled',
  claimDate, settleDate,    // 申請日・精算日
  fixedId                   // 固定費から自動生成された記録に付く
}
```

### 明細（item）
```
{ name, category, sub, amount(単価), qty(個数), taxIn(税込bool), rate(税率) }
```

### 固定費（meta.fixed の要素）
```
{
  id, name, store, category, sub,
  freq:'week'|'month'|'year',
  dow,      // freq==='week' のとき 0=日〜6=土
  day,      // freq==='month'|'year' のとき 1〜31
  month,    // freq==='year' のとき 1〜12
  start, end,                                  // end:'' は無期限
  payments:[{from,method}],                    // 日付つき支払い方法
  amounts: [{from,amount,taxIn,rate}]          // 日付つき金額
}
```

---

## 6. 主要な設計判断（なぜそうしたか）

### 6-1. 消費税
- 明細ごとに税込／税抜を持つ。**デフォルトは税込**。
- 税抜の明細を**税率ごとにまとめてから課税**し、端数は**切り捨て**。
  1品ずつ課税して丸めるより実際のレシートに近くなるため。
- カテゴリ集計では、グループの税額を明細へ金額比で按分し、端数は最後の明細に寄せる。
  → **カテゴリ別合計と総額が必ず一致する**。この性質は壊さないこと。
- 値引きはマイナス金額の明細で表現する。金額0の明細だけを保存時に除外する
  （`filter(i=>i.amount!==0)`。`>0` にすると値引きが保存できなくなる）。

### 6-2. 経費（立替）
- 状態は 申請前 → 申請中 → 精算済。状態変更時に申請日・精算日を**今日で自動記録**し、
  あとから編集画面で直せる。
- 集計は全期間が対象（立替は月をまたいで残るため）。

### 6-3. カード請求
- 締め日は**各月の末日でクランプ**する（`closePoint()`）。
  31日締めを選んだ場合、2月は28日（うるう年は29日）、4月は30日で締まる。
  これをやらないとJSのDateが翌月へ繰り上げてしまい期間がずれる。
- 「y年m月引き落とし」の `m` は**0始まり**（`getMonth()` 由来）。検算時に間違えやすい。
- 期間は「前月の締め点の翌日 〜 当月の締め点」。

### 6-4. 電子マネー（IC）
二重計上を避けるのが核心。合意した方式は次のとおり。

- **チャージは集計に入れる**（財布や口座から出た金額と月の支出合計が一致する）
- **電子マネーで払った分は集計に入れない**（チャージ時に計上済みのため）
- 残高 ＝ チャージ合計 − 利用合計 ＋ 手動調整（`method.adjust`、累積）
- 電子マネー間のチャージ（楽天キャッシュ→Suica等）は**あり得るので許可**する。
  禁止するのは**自分自身へのチャージ**のみ。
- 集計から除外する箇所は次のすべて。追加時は漏れなく対応すること。
  ホームの収支 / 集計のグラフと内訳 / 予算 / カレンダーの日別金額 / 履歴検索の合計
- **既知の未対応**: 「電子マネー」種別は前払い（チャージ式）専用。
  あと払い・iD・QUICPayなど後払いのものをここに登録すると集計から消えてしまう。
  後払いは「カード」か「口座」で登録する運用。設定画面に注意書きあり。

### 6-5. 固定費
- 支払日が来たら**実際の記録を自動生成**する（アプリ起動時に前回から今日までをまとめて作る）。
  予定として別枠に持つ方式は、集計に入らず結局手入力が要るため却下した。
- 重複防止は `fixedId` と日付の組み合わせで判定する。
- 金額も支払い方法も**日付つきで複数持てる**（「この日から◯円」「この日から◯◯で払う」）。
  先の日付を登録できるので、改定通知が来た時点で予約できる。
- 月末指定（31日）は各月の末日に丸める。
- **土日祝でずらす処理は入れない**（合意済み）。
  祝日は法改正に追随できず、自動でずれた日付が正しいか毎回確認が必要になるため。
  ずれた月だけ手で直す運用。

### 6-6. 集計と予算の関係
- **予算タブは常に「表示中の月」で集計する。** 集計タブのグラフ期間設定に影響されない。
  （過去にここを共有して、8月の予算に7月分が混ざるバグを出した）
- 予算は経費の「含む／除く」切替にも影響されない。実際に出ていったお金で見る。

### 6-7. 参照画面と編集画面
- 記録をタップすると**参照画面**が開く。編集は参照画面の「編集」から。
- **右下の＋ボタンだけ**が編集画面を直接開く（新規入力）。
- シートは常に1枚だけ開く（`open()` が他を閉じる）。重ねると表示が壊れる。

### 6-8. バックアップ
- **年ごとに `kakeibo-<年>.json`** で書き出す。名前を固定してあるので、
  同じ名前で保存すればiOSが上書き確認を出す。
- 読み込みは複数ファイルを一度に選べる。
- 案内バーは「前回から14日経過」で出る。✕で閉じてもそのセッションだけ。
- 年をまたいでも取りこぼさないよう、**年ごとの件数**（`lastBackupCounts`）を持ち、
  差分がある年をすべて書き出す。
- 保存先フォルダの指定・自動上書きは**ブラウザの権限外でできない**。
  iOSの共有シートが前回の保存先を記憶するので実用上は問題ない。

### 6-9. 入力候補（サジェスト）
- 店名・品名を過去の記録から**部分一致**で提示。datalistはiOSで挙動が弱いため自前実装。
- 品名候補は**選択中のカテゴリに一致するものを上**に出す。
- タップすると品名・金額（**直近の金額**）・カテゴリ・サブ・税設定が一括で入る。

---

## 7. 過去に踏んだバグ（再発防止）

| 症状 | 原因 | 対策 |
|---|---|---|
| 非表示にしたはずの要素が出続ける | `hidden`属性が`display:flex`に負ける | `[hidden]{display:none !important}` をCSSに置く |
| ドーナツが1色に潰れる | マイナス（値引き）を円弧に含めていた | プラスのみで比率を計算 |
| 100%のときドーナツが消える | 円弧の始点と終点が同一座標になり描画されない | `frac>=0.9999` なら`<circle>`で描く |
| 保存できるのに反映されない | 税抜が相殺され合計0 → 按分で0除算 → NaN | `g.sum===0` のガードを入れる |
| 別の年の記録を編集すると壊れる | 「表示中の年」を前提にしていた | `findEntryYear()` で実データから年を特定 |
| 残高が更新されない | 保存後・タブ復帰時に再描画していなかった | `render()`末尾と`showTab()`の両方でフック |
| タブのアイコンだけ大きい | 記号が絵文字として描画された | 記号を変え、フォントを明示指定 |

---

## 8. 改修時の検証手順（毎回実施）

```bash
cd /home/claude/site

# 1. JS構文チェック
node -e "
const fs=require('fs');const h=fs.readFileSync('index.html','utf8');
const m=h.match(/<script>([\s\S]*)<\/script>/);
new Function(m[1]);console.log('index.html JS: OK');
new Function(fs.readFileSync('sw.js','utf8'));console.log('sw.js: OK');
"

# 2. タグの開閉が合っているか
node -e "
const h=require('fs').readFileSync('index.html','utf8');
const o=(h.match(/<div/g)||[]).length,c=(h.match(/<\/div>/g)||[]).length;
console.log('div',o,c,o===c?'OK':'NG');
"

# 3. 通信コードが混入していないか（必ず0）
grep -c "fetch(\|XMLHttpRequest" index.html
```

さらに、**日付計算・税計算・残高計算を変更したときは、必ずnodeで検算してから配置する。**
締め日の月末クランプ、うるう年、年またぎ、値引きによる相殺は特に間違えやすい。

---

## 9. 作業の進め方（利用者の好み）

- 大きな仕様は、実装前に選択肢を2〜3問提示して確認する。
- 仕様の穴（年またぎ、月末、0除算、二重計上など）は、実装前に正直に指摘する。
- できないこと（保存先の指定、祝日の完全対応など）は、できない理由とともにはっきり伝える。
- 実装は`index.html`と`sw.js`の差し替えで完結させる。
- 応答は簡潔に。変更点と、実機で確認してほしい点を明示する。
- 利用者は仕様の矛盾に鋭い。指摘が正しければ素直に認めて直す。

---

## 10. 現在の画面構成

下タブ6つ。

| タブ | 内容 |
|---|---|
| ホーム | 今月の収支 / 予算の残り / ICカードの残高 / カード請求 / 最近の入出金 |
| カレンダー | 月表示（日別の支出赤・収入青）、日付タップで下に一覧。選択日で＋を押すとその日付で入力 |
| 集計 | グラフ（月／年／期間の切替、経費の含む・除く、ドーナツ＋内訳）／履歴（店名・品名・メモ横断検索＋合計） |
| 経費 | 立替の残額、状態フィルタ、チェックで一括状態変更 |
| 予算 | 月の総額とカテゴリ別。先月からコピー |
| 設定 | カテゴリ／支払い（支払い方法・収入経路・IC残高・カード請求）／固定費／税率／バックアップ |
